Before using these codes, make sure that below modules are installed:
cv2
matplotlib
mediapipe
itertools
numpy
tkinter,

If not, install following in cmd prompt or in terminal in VS code:
pip install opencv-python
pip install matplotlib
pip install mediapipe
pip install picklable-itertools
pip install numpy
pip install Everything-Tkinter


Here totally Tkinter is used as GUI
